export type BookmarkWidgetType = {
  id: string;
  iconSize: "small" | "medium" | "large" | "fit";
  maxCols: number;
  showTabs: boolean;
  showFolders: boolean;
};

export type bookmarkSliceType = {
  // bookmarks: string[];
};
