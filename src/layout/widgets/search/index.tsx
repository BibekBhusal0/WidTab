import { SearchWidgetType } from "@/types/slice/widgets";

function SearchWidget({ id, engine }: SearchWidgetType) {
  console.log(id, engine);

  return (
    <>
      <>
        <></>
      </>
    </>
  );
}

export default SearchWidget;
